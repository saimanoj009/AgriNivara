import { Link } from 'react-router-dom'; import { getAuthUser } from '../services/api';
export default function Profile(){const u=getAuthUser();return <div className="min-h-screen bg-slate-50 p-5 lg:p-10"><div className="max-w-3xl mx-auto"><Link to="/dashboard" className="text-green-700 font-bold">← Dashboard</Link><div className="bg-white border rounded-3xl p-8 mt-6"><div className="h-16 w-16 rounded-2xl bg-green-100 text-green-700 flex items-center justify-center text-2xl font-black">{(u?.name||'F').charAt(0).toUpperCase()}</div><h1 className="text-3xl font-black mt-5">Farmer Profile</h1><div className="grid sm:grid-cols-2 gap-4 mt-7"><Info t="Name" v={u?.name}/><Info t="Mobile" v={u?.mobile}/><Info t="Location" v={u?.location||'Not provided'}/><Info t="Role" v="Farm Owner"/></div><p className="mt-7 text-sm text-slate-500">Keep your signup details accurate so recommendations and support can be associated with the correct farmer account.</p></div></div></div>}
function Info({t,v}:{t:string,v?:string}){return <div className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500 font-bold">{t}</p><p className="font-bold mt-1">{v||'—'}</p></div>}
